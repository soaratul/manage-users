
const ServerNotRunning = () => {
  return (
    <div>
      It seems that server is not up and running, pelase contact to site
      administrator.
    </div>
  );
};

export default ServerNotRunning;
